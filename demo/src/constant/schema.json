{
  "$schema": "http://json-schema.org/schema",
  "$id": "Constant",
  "title": "Constant schematics.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the constant that is going to be created.",
      "x-prompt": "What is the name of your constant?"
    }
  },
  "required": ["name"]
}
